import { useRouter } from "expo-router";
import { View } from "react-native";

const FamilyScreen = () => {
    const router = useRouter();

    return (
        <View>
            {/* Add your existing component content here */}
        </View>
    );
};

export default FamilyScreen; 